# STUDENT-RECORD-MANAGEMENT-SYSTEM
A menu-driven C project to manage student records using a Singly Linked List. It supports adding, deleting, modifying, displaying, searching, sorting, and reversing records. Dynamic memory allocation with `malloc()` is used, while file handling enables saving and loading student data for future use.
